# Example environment variables for Oryum Tauros
# Copy this file to `.env` (or set the variables in your deployment/CI) and fill the real values.

# Frontend API base used by the client when contacting Manus Forge
VITE_FRONTEND_FORGE_API_URL=https://forge.manus.ai

# OAuth Server URL (Manus API for authentication)
OAUTH_SERVER_URL=https://api.manus.im

# OAuth Portal (for login/auth flow)
VITE_OAUTH_PORTAL_URL=https://auth.manus.im
VITE_APP_ID=seu_app_id_manus

# App Configuration (client)
VITE_APP_TITLE=Oryum Tauros - ERP Rural Inteligente
VITE_APP_LOGO=/logo.svg

# Analytics (optional - leave empty if not using)
VITE_ANALYTICS_WEBSITE_ID=
VITE_ANALYTICS_ENDPOINT=

# Stripe keys (use test keys for local development)
# - STRIPE_SECRET_KEY: secret key used on the server (sk_test_... or sk_live_...)
# - STRIPE_PUBLISHABLE_KEY: publishable key used on the client (pk_test_... or pk_live_...)
# - STRIPE_WEBHOOK_SECRET: signing secret for webhook verification (whsec_...)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxx

# For Vite client (prefix VITE_) — publishable key and price IDs
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxx
VITE_STRIPE_PRICE_BASIC=price_xxxxxxxxxxxxx
VITE_STRIPE_PRICE_PROFESSIONAL=price_xxxxxxxxxxxxx
VITE_STRIPE_PRICE_ENTERPRISE=price_xxxxxxxxxxxxx

# Database connection used by server and drizzle
# Example (TiDB/MySQL):
# DATABASE_URL=mysql://<user>:<password>@<host>:<port>/<database>?ssl={"rejectUnauthorized":true}
DATABASE_URL=mysql://user:password@host:4000/dbname?ssl={"rejectUnauthorized":true}

# Additional env (optional)
# NODE_ENV=development
# PORT=4000
